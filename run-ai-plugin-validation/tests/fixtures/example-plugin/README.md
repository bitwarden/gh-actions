# Example Plugin (fixture)

A minimal Claude Code plugin used by the `test-validate-ai-plugins.yml` workflow to exercise the `run-ai-plugin-validation` action end to end. Editing any file under this fixture in a pull request causes the action's change detection to treat the PR as a Claude-related change, which triggers the AI-driven validation step.

This is not a real plugin and provides no functionality.
