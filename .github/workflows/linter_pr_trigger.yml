name: Workflow Linter PR trigger

on:
  pull_request:
    paths:
      - .github/workflows/workflow-linter.yml

permissions:
  contents: read
  id-token: write # allow reporting to Slack using OIDC

jobs:
  linter:
    name: Lint
    uses: ./.github/workflows/workflow-linter.yml
