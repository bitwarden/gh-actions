name: Perform Checkmarx Scan

on:
  workflow_call: 

    inputs:
      incremental:
        description: "Perform incremental scan"
        type: boolean
        default: ${{ contains(github.event_name, 'pull_request') }}
      project_name:
        description: "Project name in Checkmarx"
        type: string
        default: ${{ github.repository }}
      upload_sarif:
        description: "Upload SARIF output to GitHub Security Center"
        type: boolean
        default: true
    secrets:
      CHECKMARX_TENANT:
        required: true
      CHECKMARX_CLIENT_ID:
        required: true
      CHECKMARX_SECRET:
        required: true

permissions: {}
  
env: 
  pull_request_target_compatible_ref: ${{ contains(github.event_name, 'pull_request') && format('refs/pull/{0}/merge', github.event.pull_request.number) || github.ref }}

jobs:
 
  sast:
    name: SAST scan
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      pull-requests: write
      security-events: write

    steps:
      - name: Check out repo
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ env.pull_request_target_compatible_ref }}

      - name: Scan with Checkmarx
        uses: checkmarx/ast-github-action@dda03dce250dead2404252f9970f35980201934c # 2.0.43
        with:
          project_name: ${{ inputs.project_name}}
          cx_tenant: ${{ secrets.CHECKMARX_TENANT }}
          base_uri: https://ast.checkmarx.net/
          cx_client_id: ${{ secrets.CHECKMARX_CLIENT_ID }}
          cx_client_secret: ${{ secrets.CHECKMARX_SECRET }}
          additional_params: |
            --report-format sarif \
            --filter "state=TO_VERIFY;PROPOSED_NOT_EXPLOITABLE;CONFIRMED;URGENT" \
            --output-path . ${{ inputs.incremental && '--sast-incremental' || '' }} \

      - name: Upload Checkmarx results to GitHub
        if: inputs.upload_sarif
        uses: github/codeql-action/upload-sarif@9e8d0789d4a0fa9ceb6b1738f7e269594bdd67f0 # v3.28.9
        with:
          sarif_file: cx_result.sarif
          sha: ${{ contains(github.event_name, 'pull_request') && github.event.pull_request.head.sha || github.sha }}
          ref: ${{ env.pull_request_target_compatible_ref }}

